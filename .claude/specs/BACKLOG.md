# Backlog

Small, independent tasks. Each row = one unit of work to hand to an implementation agent.

**Spec format (per item):**
- Short items live inline. Longer ones get a linked file `.claude/specs/NNN-slug.md`.
- Standard sections per spec: **Context** (current state + file paths), **Requirements**, **Acceptance**, **Out of scope**, **Open questions**.
- Always read `.claude/specs/PROJECT_MAP.md` first for orientation, then the CLAUDE.md at repo root.

**Status legend:** `draft` (gathering info) · `ready` (can hand to dev) · `in-progress` · `done` · `blocked`

## Items

| # | Title | Status | Spec |
|---|---|---|---|
| 001 | Persistent sessions via PdoSessionHandler (survive deploys, 30-day idle) | done | [001-pdo-session-handler.md](001-pdo-session-handler.md) |
| 002 | Admin: change another user's password (dedicated page, email + audit log) | done | [002-admin-change-user-password.md](002-admin-change-user-password.md) |
| 003 | Drop `PasswordStrength` constraint from self-service forms (keep min 8) | done | [003-drop-password-strength-constraint.md](003-drop-password-strength-constraint.md) |
| 004 | Audit log of outgoing emails (entity + listener + admin browse/detail UI) | done | [004-email-audit-log.md](004-email-audit-log.md) |
| 005 | Show/hide password toggle on every password input (Stimulus + form theme) | done | [005-password-visibility-toggle.md](005-password-visibility-toggle.md) |
| 006 | "Načíst z ARES" button next to every IČO field (refactor + JSON endpoint + Stimulus) | done | [006-ares-lookup-button.md](006-ares-lookup-button.md) |
| 007 | Flatpickr on every date input (form theme + filters + birthDate maxDate) | done | [007-flatpickr-everywhere.md](007-flatpickr-everywhere.md) |
| 008 | Order form as a Live Component (preserve inputs across map clicks, novalidate, on-blur server validation) | done | [008-order-form-live-component.md](008-order-form-live-component.md) |
| 009 | Order: hide map by default, opt-in via "auto vs. pick from map" radio | done | [009-order-storage-selection-mode.md](009-order-storage-selection-mode.md) |
| 010 | Customer documents after successful payment (success page + email link + new docs index) | done | [010-customer-documents-after-payment.md](010-customer-documents-after-payment.md) |
| 011 | Move highlighted-storage map from pre-payment to post-payment email (attachment, once at finalization) | done | [011-move-storage-map-to-post-payment-email.md](011-move-storage-map-to-post-payment-email.md) |
| 012 | Photos visible across the order journey (place card / form sidebar / map modal / recap) with GLightbox, never cropped | done | [012-photos-during-order.md](012-photos-during-order.md) |
| 013 | Price label reflects billing cadence (Celková cena vs Měsíční platba / měsíc) — fix in emails + UI | superseded by 021 | [013-price-label-by-billing-cadence.md](013-price-label-by-billing-cadence.md) |
| 014 | One-click "Prodloužit pronájem" from previous order (email CTA + portal CTA + prefilled form) | done | [014-one-click-prolong-from-previous-order.md](014-one-click-prolong-from-previous-order.md) |
| 015 | Order accept redesign — wider layout, smaller inline photos, inline signing (no modal, radios), single consolidated consent | done | [015-order-accept-redesign.md](015-order-accept-redesign.md) |
| 016 | GoPay & VOP compliance pass — button label per § 1826a OZ, dedicated recurring consent, card/3DS logos, parameter card, identification, recurring confirmation + advance-notice e-mails | ready (P1+P2) / P3 deferred | [016-gopay-vop-compliance.md](016-gopay-vop-compliance.md) |
| 017 | Per-place order expiration window (default 3 days) — replace hardcoded `RESERVATION_DAYS = 7` with a configurable `Place::$orderExpirationDays`, exposed in the place edit/create form | done | [017-per-place-order-expiration-days.md](017-per-place-order-expiration-days.md) |
| 018 | Detect customer-side cancellation of recurring (ON_DEMAND) GoPay payments — distinct event + acknowledgement e-mails (customer + admin), stop retry loop, leave contract in documented holding state | draft | [018-handle-gopay-side-recurring-cancellation.md](018-handle-gopay-side-recurring-cancellation.md) |
| 019 | Settle outstanding usage when customer cancels an open-ended recurring payment — preview prorated amount on cancel page, charge via GoPay token before voiding it, fall through to outstanding-debt path on failure | draft | [019-settle-outstanding-on-cancel.md](019-settle-outstanding-on-cancel.md) |
| 020 | Public order status permalink — signed `/objednavka/{id}/stav?_hash=…` (UriSigner) replacing `/dokonceno`; lifecycle-aware page (Aktivní/Čeká na platbu/Zrušeno/Pozastaveno/…) with state-specific CTAs, all order docs gated by signature, CTA wired into every customer-facing e-mail | done | [020-public-order-status-permalink.md](020-public-order-status-permalink.md) |
| 021 | Customer never sees a lifetime total (always "Měsíční platba / měsíc" for recurring); admin & landlord see "Zaplaceno" + "K zaplacení celkem" (only for fixed-term recurring; absent for unlimited). Subsumes 013 | done | [021-customer-price-display.md](021-customer-price-display.md) |
| 022 | Optional per-place storage access codes — Place config (enabled/digits/from/to) + new `place_storage_code_usage` history table + `StorageCodeGenerator` service + canvas "Vygenerovat" button + handover random pre-fill + bulk-fill + Reset action + customer surfacing on portal order detail and public `/stav` (gated by active contract) | done | [022-storage-access-codes.md](022-storage-access-codes.md) |
| 023 | Po splatnosti — admin overdue detection: rename hidden `payment-issues` → `/portal/admin/po-splatnosti` with severity-coloured table, admin-nav entry with live count badge, dashboard tile + top-5 preview, "Dlužník" badge + filter on user list, badges in admin orders list, warning banner on admin order detail. New `OverdueChecker` service + `OverdueContractView` + Twig extension. | done | [023-overdue-detection-for-admins.md](023-overdue-detection-for-admins.md) |
| 024 | Per-place dashboard at `/portal/places/{id}` for landlords (own storages only, with co-owner disclaimer) and admins (whole place + setup-health alerts + place-scoped Po splatnosti banner). KPI tiles (obsazenost / tržby / MRR / smlouvy), Brzy končící, Co se chystá, posledních 5 objednávek, volné sklady per typ, 12-měsíční chart. Hub kept as "Správa místa" section. New `GetPlaceDashboardStats` + `GetPlaceRevenueChart`, place-scoped repo methods, `RevenueChart.placeId` prop. Plus admin places list gains a "Detail" link. | done | [024-per-place-dashboards.md](024-per-place-dashboards.md) |
| 025 | Onboarding billing controls — `Contract.individualMonthlyAmount` (per-customer monthly override that survives storage price changes), `Contract.paidThroughDate` from external prepayment, free contracts (zero monthly, no charge, no invoice). Both onboarding flows (digital + migrate) gain a "Cenový model" section (Standardní / Individuální / Zdarma) + "Externí předplatné" date. Fixes `ChargeRecurringPaymentHandler` reading storage instead of contract. New daily cron `app:send-external-prepayment-ending-soon` (7 days before paidThroughDate). Admin order list gains badges + filter strip (Indiv. cena / Externí předplatné / Předplatné brzy končí / Zdarma); user list gains "Onboardovaný" badge + filter; order detail gains info banner. | done | [025-onboarding-billing-controls.md](025-onboarding-billing-controls.md) |
| 026 | Admin customer list — per-row rentals (active / total) + MRR (Kč/měs) columns; new filter chips `S aktivními smlouvami ({N})` and `Bez aktivních smluv ({N})` alongside existing `Vše` / `Pouze dlužníci`. One-query stats lookup on `ContractRepository::loadCustomerStatsByUserIds()` (mirrors the per-page `debtorIdSet` pattern). Recurring predicate excludes short LIMITED <28d so one-shot rentals don't inflate MRR. Independent of spec 025 — `Order.firstPaymentPrice` is already the locked-in monthly. | done | [026-admin-customer-list-rentals-mrr.md](026-admin-customer-list-rentals-mrr.md) |
| 027 | Rental visibility & planning ("od kdy / do kdy") — new `StorageOccupancyService` + `StorageRentalView` / `RentalSpan` value objects feed: (a) calendar gains `končí`/`začíná` per-day badges, expandable per-day detail, and a new "Časová osa" Gantt tab; (b) storage list (with type selected) gains `Nájemce / Pronajato OD / DO / Příští volný` columns + `Plánování (90 dní)` button; (c) storage type list gains `Celkem / Obsazeno / Volných / Nejbližší volný` columns + `Plánování` link; (d) place detail's "Volné sklady (per typ)" replaced by richer "Obsazenost typů" with `Nejbližší uvolnění do …`; (e) NEW per-type planning page `/portal/places/{placeId}/storage-types/{id}/obsazenost` with KPI tiles, sortable rentals table, 90-day mini-timeline strip per storage. Single "do kdy" rule: `terminatesAt` > `Contract.endDate` > `Order.endDate` > `neomezeně`. Owner-scoped for landlords. | done | [027-rental-visibility-and-planning.md](027-rental-visibility-and-planning.md) |
| 028 | Excel export of admin & landlord list pages — new `composer require openspout/openspout` + thin `ExcelExporter` service (streams XLSX, 50 000-row cap, type-aware cells: money / date / boolean Ano/Ne) + `ExcelSheet` value object. Adds `Export do Excelu` button + paired single-action export controller to: admin orders (with filter), admin places, users (with filter), po-splatnosti, audit log (with filter), email log (with filter), landlord orders, landlord self-billing, landlord per-place storage list. Export ALWAYS reuses the source list's repository method + security gate — landlord exports are strict-ownership only (`PlaceAccess` does not widen). Czech enum labels via new `OrderStatus::label()` / `UserRole::label()` / etc. helpers (also used to dedupe inline maps in templates). Admin-large endpoints stream via `Query::toIterable()` + `EM::clear()` for bounded memory. | done | [028-excel-export-list-pages.md](028-excel-export-list-pages.md) |
| 029 | Searchable, grouped admin dropdowns (TomSelect rollout) — wraps storage / storage-type / place selects + admin filter `<select>`s in opt-in `data-controller="tom-select"`. Adds TomSelect 2.x via importmap, a single Stimulus controller, and small Tailwind overrides. New `App\Service\Form\StorageChoiceBuilder` produces `place — type` optgroups with natural-sorted storage numbers. Refactors `AdminCreateOnboardingFormType`, `AdminMigrateCustomerFormType`, `StorageUnavailabilityFormType`, `StorageFormType` (place + storage-type pickers); annotates raw `<select>` filters on audit-log, email-log, calendar, and landlord storage list (calendar/storage list autosubmit migrates from inline `onchange` to controller `requestSubmit`). Diacritic-folding (`praha` matches "Praha") + group-label search (`searchField: ['text', 'optgroup']`). Consumer flows + radio enums + canvas drawer + RevenueChart Live select explicitly out of scope. | done | [029-tom-select-admin-dropdowns.md](029-tom-select-admin-dropdowns.md) |
| 030 | Customer-facing reflection of free / external-prepayment contracts — new shared partial `templates/components/customer_billing_status.html.twig` rendering one of (a) green "Pronájem zdarma" badge for `Contract.individualMonthlyAmount === 0`, (b) blue "Předplaceno externě do dd.mm.yyyy" banner when `paidThroughDate` is >7 days away and `goPayParentPaymentId` is null, (c) amber "Externí předplatné brzy končí" banner with `simek@fajnesklady.cz` CTA when 0..7 days remain. Included in `templates/portal/user/order/detail.html.twig` and `templates/public/order_status.html.twig`. New `Contract::daysUntilExternalPrepaymentEnds(\DateTimeImmutable): ?int` helper (returns null when not externally prepaid / already converted / terminated). View-model gains a `now` field via `ClockInterface` injection in the factory. Admin / landlord views explicitly out of scope (already covered by spec 025). | done | [030-customer-billing-status-display.md](030-customer-billing-status-display.md) |
| 031 | Daily overdue admin e-mail digest — new cron `app:send-overdue-digest-email` (08:00 Europe/Prague), one e-mail per `ROLE_ADMIN` when overdue count > 0, top 10 contracts by severity, CTA to `/portal/admin/po-splatnosti`. New `OverdueDigestSent` entity + repo for per-day per-admin idempotency (unique constraint on `(admin_id, date)`). New `OverdueDigestRequested` event + `SendOverdueDigestEmailHandler`. New email template `overdue_digest.html.twig`. Re-uses `OverdueChecker::summarise()` + `findOverdueViews()` from spec 023; mirrors cron pattern of spec 025's `app:send-external-prepayment-ending-soon`. | done | [031-overdue-admin-email-digest.md](031-overdue-admin-email-digest.md) |
| 032 | Append-only `ContractPriceChange` audit trail for `Contract.individualMonthlyAmount` — every call to `applyIndividualMonthlyAmount` records a row with previous/new amount, timestamp, `changedBy:User?`, and free-text `reason?`. New `ContractPriceChanged` domain event recorded by `Contract` (gains `EntityWithEvents` + `HasEvents`); persisted via `PersistContractPriceChangeHandler`. Method signature extends to `(?int $amount, ?User $changedBy, ?string $reason, \DateTimeImmutable $now)`; only production caller is `OrderService::completeOrder()` (one site). New `Order.createdByAdmin` FK propagates the actor from onboarding handlers. Migration backfills one row per pre-existing override-bearing contract (`previousAmount=NULL, reason='Initial value (backfill)'`). Admin order detail gains read-only "Historie ceny" panel beneath the onboarding banner. No edit UI ships — foundation only. | done | [032-contract-price-change-audit.md](032-contract-price-change-audit.md) |
| 033 | Per-place "Návod" document (admin-managed) — new optional `Place.instructionsPath` (PDF/DOCX, max 10 MB) uploaded to `places/{id}/instructions/…` via `PlaceFileUploader::uploadInstructions()`. Field rendered in `PlaceFormType` only when `is_admin: true` (admin-only via new form option; landlords still own Provozní řád). Attached as `navod.{ext}` to the post-payment `contract_ready` e-mail (sibling to operating rules). Surfaced as a "Návod pro zákazníky" download row in both `order_documents.html.twig` (portal) and `order_status_documents.html.twig` (public `/stav`). Place lists (portal + admin) gain a second amber triangle next to the existing one; per-place dashboard gains a `missingInstructions` health alert via new bool on `GetPlaceDashboardStatsResult`. Inventory row added to `.claude/CUSTOMER_DOCUMENTS.md`. Explicitly NOT part of order-acceptance legal consent. | done | [033-place-instructions-document.md](033-place-instructions-document.md) |
| 034 | Critical billing bug — Fakturoid was double-counting VAT (gross price sent as `unit_price` with `vat_rate: 21` and default `vat_price_mode`, so 600 Kč was invoiced as 726 Kč). Fix: send `vat_price_mode: 'from_total_with_vat'` on every `create...Invoice` call in `FakturoidApiClient` (consumer order, recurring, self-billing). Adds a focused unit test on the payload shape. Out of scope: correcting already-issued wrong invoices (manual in Fakturoid UI), self-billing rate-vs-VAT-payer-status. | done | [034-fakturoid-vat-double-counted.md](034-fakturoid-vat-double-counted.md) |
| 035 | Dynamic per-order VOP — operator-maintained DOCX template at `templates/documents/vop_template.docx` with two placeholders: `${PRICELIST_URL}` (resolved per-order from `public_place_pricelist` → `/pobocka/{id}/cenik`) and `${OPERATING_RULES_URL}` (absolute URL of `Place.operatingRulesPath` via `UrlHelper`, falling back to `public_place_detail` when no rules uploaded). Reuses existing `DocumentPdfConverter` (LibreOffice), downgrades PDF 1.7 → 1.4 via in-container Ghostscript, then stamps `Order.signaturePath` PNG at bottom-right of body pages via new `setasign/fpdi` + `setasign/fpdi-tcpdf` (TCPDF already present). `skipLastPages=2` keeps the two fillable form annexes (withdrawal + complaint) unsigned. New `App\Service\Vop\VopDocumentGenerator` (persists DOCX to `var/vop/vop_{orderId}.docx`) + `VopPdfStamper`. New `OrderVopDownloadController` (public, UriSigner-gated) + `VopPdfController` (auth, `OrderVoter::VIEW`). `OrderStatusUrlGenerator` gains `generateVopDownload`. `SendOrderConfirmationEmailHandler` swaps the static `vop.pdf` attachment for the per-order signed PDF; static `public/documents/vop.pdf` removed. On-page legal modals and the other two static legal PDFs explicitly out of scope. | done | [035-dynamic-signed-vop-per-order.md](035-dynamic-signed-vop-per-order.md) |
| 036 | Manual recurring payments — new `BillingMode` enum (ONE_TIME / AUTO_RECURRING / MANUAL_RECURRING) on `Order` + `Contract`; eligible only for fixed-term LIMITED ≥ 28 days (UNLIMITED forced AUTO, short LIMITED forced ONE_TIME). MANUAL track: first payment one-shot GoPay (no token saved), then new daily cron `app:send-manual-billing-payment-requests` emails a one-time GoPay link N days before each `Contract.nextBillingDate` (default -7/-2/0) plus +3/+7 overdue chases to customer + admin. **Reminder offsets live on `Place` (5 flat int columns, admin-only form panel) and snapshot onto `Order` at creation — running orders keep the schedule they were placed under.** `ManualBillingReminderSchedule` becomes a value object built via `fromOrder()` / `fromPlace()`. New `ManualPaymentRequest` entity (one per `(contract, periodStart)`, unique constraint) tracks `goPayPaymentId` + per-stage send timestamps in a `sentStages` JSON map. **Idempotency at 3 layers**: schema unique constraint, `sentStages` gate, `SELECT … FOR UPDATE` lock in the per-stage `DispatchManualBillingNotificationCommand` handler (record-stage-then-dispatch order via `doctrine_transaction`, so re-runs / parallel cron / crash mid-loop never double-send). Webhook handler reconciles via new branch in `ProcessPaymentNotificationHandler` → reuses `Contract::recordBillingCharge` + `RecurringPaymentCharged` event so invoice + Payment row issue exactly as for AUTO. Customer choice surfaces on `/objednavka` Live Component (schedule preview re-labels reactively); `/prijmout` hides the recurring-payment disclosure card + dedicated consent checkbox for MANUAL. Admin onboarding gets a "Způsob následných plateb" radio (orthogonal to existing EXTERNAL/GOPAY `paymentMethod`). MRR / "active recurring" predicates in 8 `ContractRepository` methods widen from `goPayParentPaymentId IS NOT NULL` → `billingMode IN (auto_recurring, manual_recurring)` so landlord/admin dashboards reflect MANUAL revenue. New customer billing-status partial variants (pending / scheduled). Mode locked at creation (no admin switch UI); externally-prepaid-ending-soon cron skips MANUAL contracts. | done | [036-manual-recurring-payments.md](036-manual-recurring-payments.md) |
| 037 | Validate billing address against a real-world registry (CZ-first) — new `App\Service\Address\AddressValidator` (Photon-backed, `cache.app` 7d) + `App\Controller\Api\AddressSuggestController` proxy (rate-limited `address_suggest`, 300/h/IP) + `assets/controllers/address_autocomplete_controller.js` (debounced typeahead, fills street/city/PSČ, dispatches `input`/`change` for Live Components) + Twig macro `_address_override.html.twig` (`render()` + `render_live()`). New class-level `#[AddressExists]` constraint + `AddressExistsValidator` (autowires the service — POPO FormData can't DI) + `HasBillingAddress` property-hook interface implemented by all 7 affected FormData classes. Soft-warn UX: on `notFound`, violation lands at path `billingStreet` and an `addressOverride` CheckboxType (mapped, hidden until violation surfaces) lets the user proceed. Photon outage → `skipped` (never blocks orders). Applied to: order Live Component, BillingInfoForm Live Component, profile-edit (`AdminUserFormData`), admin onboarding digital + migrate, customer register, landlord register. Mirrors spec 006 (ARES) skeleton verbatim — same controller pattern, rate-limiter convention, Stimulus structure. Mapy.cz Suggest rejected (needs API key); RÚIAN data dump rejected (overkill). | done | [037-address-existence-validation.md](037-address-existence-validation.md) |
| 038 | Address formatter + "Navigovat" CTA for places without street — fixes the malformed `, City` text that surfaces wherever a place has `address: null` but coords (e.g. `PlaceFixtures::REF_PLZEN`); 7 templates + 6 email handlers today blindly string `address, postalCode city`. New `App\Service\Place\PlaceAddressFormatter` (`format(Place): string`, `navigationUrl(Place): ?string` → Google Maps navigation intent `https://www.google.com/maps/dir/?api=1&destination=lat,lng`, falls back to URL-encoded address string when no coords) + new `App\Twig\PlaceAddressExtension` exposing `place_address` / `place_navigation_url` / `place_has_navigation`. New on-page partial `templates/components/place_address.html.twig` (renders address line + optional Navigovat button, `inline` + `with_navigate` flags) used on portal user / landlord / admin order detail, public `/stav` (×2), public `/prijmout` (inline, no CTA), admin place list (inline), storage canvas heading. New email partial `templates/email/_place_navigate_button.html.twig` (table-safe inline-CSS button) wired into all 6 customer-facing email templates (`order_placed`, `rental_activated`, `order_cancelled`, `contract_expiring`, `storage_availability_warning` + admin variant) whose handlers now also pass `placeNavigationUrl`. `AdminPlaceExportController` refactored to the new service. List rows / canvas get the malformed-text fix only (no Navigovat CTA per user scope). | done | [038-place-address-formatter-and-navigate-cta.md](038-place-address-formatter-and-navigate-cta.md) |
| 039 | Quick-duration buttons (`1 měsíc / 3 měsíce / 6 měsíců`) next to the order form's endDate for LIMITED rentals. Pure client-side via new `assets/controllers/duration_preset_controller.js` mirroring the ARES button pattern; reads startDate input, computes `startDate + N months` with last-of-month clamping, calls flatpickr `picker.setDate(date, true)` on endDate, forwards a `blur` so Live UX's `validateField` fires + price preview re-renders. Buttons disabled (greyed + hint "Nejdříve zvolte datum začátku.") until startDate is set. Gated by existing `rentalIsLimited` Twig branch in `templates/components/OrderForm.html.twig`. No backend changes — `OrderFormType`, `OrderFormData`, `OrderCreateController` untouched. Preset cap at 6 months keeps every duration well inside the 7-day floor + 1-year ceiling already enforced by `validateDates`. | done | [039-quick-duration-buttons-order-form.md](039-quick-duration-buttons-order-form.md) |
| 040 | Order-form Ceník panel collapses to the rate that will actually apply once the customer's selections lock it in — UNLIMITED → monthly; LIMITED < 28 days → weekly; LIMITED ≥ 28 days → monthly. Falls back to "both rates + explainer" while LIMITED dates are missing/invalid so customers can comparison-shop. Adds one method (`OrderForm::getApplicableRate(): ?string`) mirroring `PriceCalculator::WEEKLY_THRESHOLD_DAYS` cutover; updates one Twig block in `templates/components/OrderForm.html.twig` (lines 441-455). Updates reactively via existing Live UX blur cycle — no new Stimulus, no client-side math, no backend changes. | done | [040-cenik-show-only-applicable-rate.md](040-cenik-show-only-applicable-rate.md) |
| 041 | "Předávací protokol" email link 403 fix — split security model by audience. Tenant: new public signed route `/predavaci-protokol/{id}?_hash=…` via `App\Service\Handover\HandoverUrlGenerator` (mirrors spec 020's `OrderStatusUrlGenerator` 1:1) + new `Public\HandoverViewController` (signature is the authorization, no voter check) + new `templates/public/handover_view.html.twig`. `SendHandoverRequestToTenantHandler` + `SendHandoverReminderToTenantHandler` swap to the signed URL — passwordless customers (created by `GetOrCreateUserByEmailHandler`) can now finally fill the protocol. Idempotency comes from `HandoverProtocol::completeTenantSide()` throwing on second submit. Landlord/admin: routes stay auth-gated; new `App\Service\Security\HandoverAccessDeniedListener` (`kernel.exception` @ priority 16) catches `AccessDeniedException` for `portal_landlord_handover_*` + `portal_user_handover_view` only — calls `Security::logout()` to clear any existing wrong-account token (otherwise `LoginController` bounces to home) and redirects to `/login?_target_path=<current>`. Listener strictly ignores `AccessDeniedHttpException` so bad-signature 403s don't get redirected. Companion audit confirmed every other customer-facing email is already passwordless-safe via spec 020 + per-row signing tokens. | done | [041-handover-email-link-access.md](041-handover-email-link-access.md) |
| 042 | Close spec 021's last gap — drop the "Cena celkem (N platby): TOTAL Kč" header from the fixed-term ≥ 28-day branch of `templates/components/OrderForm.html.twig` (lines 285-300). Replace it with "Měsíční platba: X Kč / měsíc" mirroring the unlimited branch; keep the per-payment breakdown `<ul>` exactly as is (informational dates + per-charge amounts, never summed). All other customer-facing surfaces already route through `_price_label.html.twig` / `email/_price_label.html.twig` and were verified clean (audit table in the spec); the admin/landlord `_price_aggregate.html.twig` stays role-gated and untouched. Single-file change + focused test. | done | [042-hide-lifetime-total-in-order-form-schedule.md](042-hide-lifetime-total-in-order-form-schedule.md) |
| 043 | Onboarding journey, end-to-end clarity — every customer-facing surface (signing-link e-mail, signing page, completion page, rental-activated e-mail) tells the same situation-aware story across the four billing situations (GoPay first charge / externally prepaid / free / migrate). Central abstraction `App\Service\Order\CustomerBillingSituation` enum (free wins over prepaid, prepaid wins over GoPay — mirrors spec 030's detection); feeds four content-VO siblings (`SigningPriceViewModel`, `SigningEmailContent`, `CompletionPageViewModel`, `RentalActivatedEmailContent`). Signing page reads `Order.firstPaymentPrice` (locked-in monthly) instead of recomputing from storage; prepaid/free swap the price block for a green banner. Completion page passwordless-safe (status URL instead of `/login`). `SendOrderPlacedEmailHandler` early-returns when `isAdminCreated && status=COMPLETED` (kills duplicate "Potvrzení objednávky" for one-transaction onboardings). `OrderEmailAttachmentsService` falls back to `Contract.documentPath` when `Order.hasSignature()` is false → migrate paper contract finally reaches the customer's inbox. Reframes "external = prepaid": admin form rejects `EXTERNAL + standard + no paidThroughDate` via new `validateExternalIsPrepaid` callback. New entity `OnboardingReminderSent` + daily cron `app:send-onboarding-payment-reminders` (D+2 / D+5 nudges for signed-but-unpaid GoPay onboardings; spec 036's per-stage pessimistic-lock idempotency). Surgical fix to `ContractService::calculateOutstandingDebt` (use `getEffectiveMonthlyAmount`, mirror spec 025's drift). | done | [043-onboarding-flow-qa-pass.md](043-onboarding-flow-qa-pass.md) |
| 044 | Research-only "Předpokládaná doba pronájmu" radio on UNLIMITED orders — new `App\Enum\ExpectedDuration` (3 buckets: 3–6 m / 6–12 m / >1 r) + nullable `Order.expectedDuration` column + required-when-UNLIMITED validation in all three creation forms (public `OrderFormType`, `AdminCreateOnboardingFormType`, `AdminMigrateCustomerFormType`). Threaded through `CreateOrderCommand` + `OrderService::createOrder()` (with defensive `UNLIMITED ===` guard so LIMITED can never poison the column). Displayed inline next to "Typ pronájmu" on admin / landlord / customer order detail pages — `{% if order.expectedDuration %}` hides the row for legacy NULLs and LIMITED orders. No pricing, billing, or business-rule impact — pure operator-facing research signal. | done | [044-expected-duration-for-unlimited-orders.md](044-expected-duration-for-unlimited-orders.md) |
| 045 | Yearly pricing tier — optional `StorageType.defaultPricePerYear` + nullable `Storage.pricePerYear` override (mirrors weekly/monthly); customer-chosen `Order.paymentFrequency` (MONTHLY \| YEARLY, existing enum + nullable column finally wired up) + new mirror `Contract.paymentFrequency`. New "Frekvence platby" radio on order form when eligible (UNLIMITED, or LIMITED ≥ `YEARLY_THRESHOLD_DAYS = 360`); YEARLY is **always MANUAL_RECURRING** — first charge one-shot GoPay, subsequent yearly charges via spec 036's existing manual-payment-request cron anchored on `Contract::getBillingCadenceStep()` (`+1 year` instead of `+1 month`). Sidesteps GoPay 15 000 Kč ON_DEMAND cap → no Podmínky PDF re-issuance. Customer-facing surfaces lead with "X Kč / měsíc" + grey "(účtováno jednou ročně, Y Kč)" annotation per user brief. PriceCalculator gains a yearly branch (`buildPaymentSchedule` walks `+1 year` with prorated tail at yearly daily rate; falls back to `monthly × 12` when type has no yearly set). Ceník panel extends to a 3-row applicable-rate collapse. Recurring-payment consent checkbox on `/prijmout` suppressed for YEARLY. Admin onboarding flows (digital + migrate) get the same selector, gated on the chosen type having a yearly rate. MRR excludes yearly (would inflate 12×); sibling YRR metric tallies yearly amounts on user list, place detail, and admin places export. `CompleteOrderHandler` also seeds MANUAL contracts' first `nextBillingDate` (yearly-required, incidental monthly-MANUAL bugfix). | done | [045-yearly-pricing-tier.md](045-yearly-pricing-tier.md) |
| 046 | Handover protocol visibility & admin Operations hub — surface `HandoverProtocol` from every order detail (customer portal + public `/stav` + landlord + admin) via a shared status-aware banner `templates/components/handover_banner.html.twig` (amber for pending/partial, green for completed) keyed on `actor: 'tenant'\|'landlord'\|'admin'`; passwordless customers reach a freshly-minted signed tenant URL from `/stav` (re-uses `HandoverUrlGenerator::generateTenantView()`). NEW admin read-only view at `/portal/admin/predavaci-protokol/{id}` (`AdminHandoverViewController`) showing timeline + both submitted sections + photos. NEW `HandoverStatus::label()` / `isWaitingOn(actor)` / `badgeClass()`. NEW Operations hub at `/portal/admin/operace` (`AdminOperationsController`) bundling 4 sections: Předávací protokoly (incl. > 14 d red-row "overdue"), Smlouvy končící bez protokolu (≤7 d), Onboarding podepsaný bez platby (reuses spec 043's `findOnboardingReminderCandidates`), Externí předplatné brzy končící (reuses spec 025's query) + link-out summary card for Po splatnosti. Backed by new `OperationsAlertsBuilder` + `OperationsAlertSummary` VO, new `HandoverProtocolRepository::findPending()` / `countPending()`, new `ContractRepository::findExpiringWithoutProtocol()`. NEW sidebar nav "Operace" with red count badge via new `OperationsExtension::operations_alerts_count()` (scalar SQL, mirrors `OverdueExtension`); admin dashboard gains a sibling alert card next to Po-splatnosti. Documents panel (`order_documents.html.twig` + `order_status_documents.html.twig`) gains a "Předávací protokol — Vyplněn dd.mm.yyyy" archival row when `COMPLETED`. PDF export of completed protocol + admin "nudge"/"complete-on-behalf" actions explicitly out of scope. | done | [046-handover-visibility-and-admin-operations-hub.md](046-handover-visibility-and-admin-operations-hub.md) |
| 047 | Per-place operations split — slim `/portal/places/{id}` to warnings + 3 grouped clickable KPI cards (Obsazenost / Tržby / Smlouvy) + Správa místa hub + danger zone; move all tables/chart to dedicated sub-pages. New `/portal/places/{placeId}/obsazenost` with **Mapa obsazenosti**: read-only Konva canvas reuses `storage_map_controller.js` in a new `viewMode='occupancy'` branch (adds `storagesValueChanged()` callback + tenant/until tooltip + modal); wrapped in new `PlaceOccupancyMap` Live Component with `viewDate` LiveProp (Dnes / +7 / +30 chips + flatpickr) — server pre-computes status + tenant + smlouva DO + orderUrl per storage via `StorageOccupancyService::currentViews($storages, $viewDate)`. New `/portal/places/{placeId}/finance` (chart + 4 KPI tiles + 12-row monthly table) and `/portal/places/{placeId}/smlouvy` (chip-filtered active / brzy končící / nadcházející / posledních 20). New `ContractRepository::findActiveAtPlace` + `PaymentRepository::sumAtPlaceForRange`. Both place lists (`/portal/places` + `/portal/admin/places`) aligned to `Název (clickable) \| Typ \| Adresa \| Stav \| Vytvořeno \| Akce`. | done | [047-per-place-operations-sub-pages-and-occupancy-map.md](047-per-place-operations-sub-pages-and-occupancy-map.md) |
| 048 | Homepage map UX redesign — wrap `#pobocky` in `max-w-6xl` container, replace verbose cards with compact two-line `Název \| Od XX Kč / od X,X m² \| K dispozici/Obsazeno` cards (drop type pills + address row + explicit CTA), switch Leaflet popovers from above-pin to **side popovers** (auto-flip near right edge) with simplified content (only available types, binary badge, no counts). Mobile (<768 px): pin tap opens **fullscreen modal** with same content + close ×. Mobile (<1024 px): list above map is hidden and replaced by a floating **`Seznam poboček (N)`** pill that opens a bottom sheet. New "Najít nejbližší pobočku" button + `localStorage` consent (`fajnesklady.geolocation.consent`) — on grant, blue user-dot marker + haversine re-sort + flyTo + top-right "Nejbližší: X (≈ N km)" chip when no marker is in viewport. **`%` availability ratio computed server-side as internal sort key only — never user-visible**; cards/popovers expose binary `K dispozici` / `Obsazeno` only. Sold-out pins fade via opacity+grayscale (no color tier). `HomeController` enriches `$placesData` with `isAvailable / lowestPrice / lowestAreaM2` and pre-sorts both arrays by `(ratio DESC, capacity DESC, name ASC)`. No new entities/commands/routes. | done | [048-homepage-map-ux-redesign.md](048-homepage-map-ux-redesign.md) |
| 049 | FIO bank transfer payments — `PaymentMethod::BANK_TRANSFER`, auto-generated 10-digit variable symbol (CRC32 of UUID, unique index), FIO API polling cron (`app:process-fio-transactions`, `mhujer/fio-api-php`), QR code (SPD via `rikudou/czqrpayment`), global admin-configurable surcharge (default 100 CZK/month, `PlatformSettings` entity), `BankTransaction` + `BankAccountMapping` entities for reconciliation + manual pairing, admin "Bankovní platby" page (unmatched badge), order form payment method choice, bank transfer payment page, reminder emails with bank details + QR for MANUAL_RECURRING, admin onboarding BANK_TRANSFER option with optional VS override, admin order detail bank payments panel | done | [049-fio-bank-transfer-payments.md](049-fio-bank-transfer-payments.md) |
| 050 | Unified admin onboarding — single dynamic Live Component form (replaces digital + migrate), inline Konva storage map with availability filtering, cascading place → type → dates → storage, customer always signs VOP via `/podpis/{token}`, fix BANK_TRANSFER redirect to payment page, payment CTA on `/stav`, optional contract upload (paper → VOP-only signing), variable symbol override | done | [050-unified-admin-onboarding.md](050-unified-admin-onboarding.md) |
| 051 | Onboarding debt — optional "Dluh" field on admin onboarding; separate sequential payment step (GoPay/bank transfer) after VOP signing, before first rental; immediate + D+2/D+5 reminder e-mails; debt CTA on `/stav`; auto-complete for free/prepaid after debt paid; EXTERNAL blocked when debt > 0 | done | [051-onboarding-debt.md](051-onboarding-debt.md) |
| 052 | Four-tier pricing — split single monthly into short-term (1–6 měs.) + long-term (6+ měs. / unlimited); weekly threshold 28→31 days; yearly + long-term monthly now required on StorageType; "Zvýhodněná cena" badge on yearly; Ceník panel shows applicable tier; migration backfills long-term = current monthly, yearly = monthly × 12 | done | [052-four-tier-pricing.md](052-four-tier-pricing.md) |
| 053 | Contractual fines (smluvní pokuty) — `Fine` entity + `FineType` enum (dirty storage 6 000 Kč / non-return 2 000 Kč/day / late payment 0.25 %/day min 250 Kč / custom), admin issues with predefined auto-calc + editable amount + mandatory note, customer pays via GoPay redirect or bank transfer QR (unique VS per fine), webhook + FIO auto-matching, fine issued + paid + reminder emails, admin fines list + export + order detail panel + cancel, customer sees on `/stav` + portal with payment CTA, operations hub section + sidebar badge, D+7/D+14 reminder cron | done | [053-contractual-fines.md](053-contractual-fines.md) |
| 054 | Order form email existence check — on email blur, Live Component checks `UserRepository::findByEmail`; if found, hides password field and shows blue info banner ("Účet s tímto e-mailem již existuje…"); reappears when email changed to unknown; `submit()` forces `plainPassword = null` defensively | done | [054-order-form-email-existence-check.md](054-order-form-email-existence-check.md) |
| 055 | Contract termination overhaul — fix admin 403 (new AdminContractTerminateController with immediate/with-notice radio), fix ProcessContractTerminationsCommand flush bug (terminations never persisted), VOP XI compliance (formal "Výzva k úhradě" email at day 3, termination at day 7 not day 10), admin notification on customer termination, reason-aware termination emails with VOP X obligations (15-day vacating), pending-termination banner on order details, new Contract.paymentDemandSentAt field | done | [055-contract-termination-overhaul.md](055-contract-termination-overhaul.md) |
| 056 | Order email history + manual send actions — nullable `orderId` FK on `EmailLog` (via `X-Order-Id` header), inline email history table on admin order detail, 4 manual action buttons (resend signing link, onboarding payment reminder, manual billing request, fine payment reminder), `orderId` filter on email log page | done | [056-order-email-history-and-manual-actions.md](056-order-email-history-and-manual-actions.md) |
| 057 | Interactive "Jak to funguje?" wizard modal — step cards become clickable CTAs opening a 3-step wizard (place → storage type → order CTA). Geolocation-aware: top 3 nearest places when granted, all places when denied. Pure Stimulus, no backend changes — reads existing `placesData` JSON. Step indicator, back nav, Escape close, mobile-friendly | done | [057-interactive-how-it-works-wizard.md](057-interactive-how-it-works-wizard.md) |
| 058 | Contract always fixed-term ("doba určitá") with auto-extension per VOP §IV — UNLIMITED contracts get `endDate = startDate + billingCadenceStep` (never null), `recordBillingCharge` advances endDate, `isActive()` grace period for recurring past endDate, contract DOCX always "na dobu určitou", `findDueForTermination` excludes auto-extending contracts, customer termination = cancel recurring → expire at endDate, ~20 template updates, backfill migration for existing UNLIMITED contracts | done | [058-contract-always-fixed-term-with-auto-extension.md](058-contract-always-fixed-term-with-auto-extension.md) |
| 059 | Complete order/contract audit trail — fill 14 gap actions (recurring charge/failure, fine lifecycle, handover protocol, admin manual emails, payment demand), add `orderId` + `userIdContext` FKs to `AuditLog` with backfill migration, unified "Historie aktivity" timeline on admin order detail (audit + email merged), `orderId` filter on admin audit log page | done | [059-complete-order-audit-trail.md](059-complete-order-audit-trail.md) |
| 060 | Admin dashboard redesign — clickable KPI cards linking to contextual admin pages, remove redundant "Nejvíc po splatnosti" table + "Správa uživatelů" section, compact user stats row with "Neověření" badge (new `?filter=unverified` on user list), add "Poslední objednávky" (last 5 orders) section | done | [060-admin-dashboard-redesign.md](060-admin-dashboard-redesign.md) |
| 061 | Address autocomplete parity — admin onboarding form adopts the shared `addr.render_live` macro (Photon dropdown + click-to-fill + soft-warn override, zero backend work); city field also triggers suggestions with dropdown anchored to the active input; PSČ gets `inputmode=numeric` + autofill tokens across all five address forms via the macro. One address (three editable columns) stays the robust source of truth — autocomplete is enhancement only, no single-field collapse, no second/company address. | done | [061-address-autocomplete-onboarding-parity.md](061-address-autocomplete-onboarding-parity.md) |
| 062 | Native input types & autofill — order form gets full native `type`/`inputmode`/`autocomplete` coverage (IČO numeric, company-name `organization`, …); admin onboarding form gets keyboard hints (`inputmode`) but browser autofill **suppressed** (`autocomplete="off"`) so the admin's own identity isn't injected into customer fields. Shared address macro gains a `browserAutofill` flag (default on; onboarding passes off) so Photon dropdown stays but browser address autofill is suppressed on the admin form. | done | [062-native-input-types-and-autofill.md](062-native-input-types-and-autofill.md) |
| 063 | Re-validate date fields on calendar pick — Flatpickr `onChange` fires no blur, so the blur-wired live `validateField` never re-runs and a stale "v minulosti" error lingers until an extra click. One-line fix in the shared `datepicker_controller.js`: dispatch `blur` on the hidden input from `onChange` (mirrors the existing alt-blur forward). Covers startDate/endDate/birthDate, no template churn. | done | [063-datepicker-validate-on-change.md](063-datepicker-validate-on-change.md) |
| 064 | Link start/end date pickers (order + onboarding) — picking a start date instantly constrains the end picker to [start+7d, max] client-side (max = start+1y on order form, unbounded on onboarding where admins may backdate & set longer terms); end picker disabled until a start exists; a start change that invalidates an already-picked end clears it. New public methods on `datepicker_controller.js` (`setRange`/`clearIfOutsideRange`/`setEnabled`), driven from the existing `duration_preset_controller.js` (its button/hint logic no-ops when those targets are absent, so onboarding reuses it without preset buttons); rule numbers passed as Stimulus values. Server `validateDates` stays authoritative. | done | [064-linked-start-end-date-constraints.md](064-linked-start-end-date-constraints.md) |
| 065 | User detail (`/portal/users/{id}`) — add a financial overview card (active/total contracts, MRR, YRR, total debt, po-splatnosti badge) + a table of all the user's orders (reference, status, what/where = storage type + number + place, period, price, per-order debt/past-due/onboarding-debt indicators). New `OrderRepository::findByUserWithDetails`, `ContractRepository::findByOrderIds`, `OverdueChecker::findOverdueViewsForUser`. Read-only; row links to admin order detail. | done | [065-user-detail-orders-and-finance.md](065-user-detail-orders-and-finance.md) |
| 066 | User list (`/portal/users`) — free-text search (name/email/phone) + click-to-sort columns (name, email, created, smlouvy, MRR, YRR) over the **whole** dataset + keep filter chips, all composable. Replaces the 6 ad-hoc per-filter repo methods with one enriched SQL query (`findForAdminList`/`countForAdminList` + `UserListCriteria`/`UserListRow`). Export honors filter+search. **Large.** | done | [066-user-list-search-and-sortable-grid.md](066-user-list-search-and-sortable-grid.md) |
| 067 | Canonical order reference number — unify the three inconsistent customer-facing "numbers" (order_placed bare uuid8, contract DOCX order-derived `Y-md-UUID8`, rental/expiring emails contract-derived) into ONE order-derived `Y-md-UUID8` via a shared `OrderReferenceFormatter`, reused across emails + contract + public status + admin grids. No migration; contract PDF unchanged. Prereq for searchable/displayed order number. | done | [067-canonical-order-reference-number.md](067-canonical-order-reference-number.md) |
| 068 | Orders list (`/portal/admin/orders`) — show canonical order reference as "Číslo" column, per-order debt / Po-splatnosti / onboarding-debt badges (via order→contract map + page-scoped overdue contract ids), and search by order number (matches order-id & contract-id prefixes for back-compat) or customer name/email, composing with filters. Export parity. Depends on 067 (+ reuses 065's `findByOrderIds`). | done | [068-orders-list-debt-and-search.md](068-orders-list-debt-and-search.md) |
| 069 | Fine form money inputs in koruny, not haléře — the fine create form is the **only** haléře input left (audit confirms all other money inputs are already CZK floats). Switch `FineFormData` amount + late-payment-base to `?float …InCzk` (`NumberType` scale 2, `inputmode=decimal`), convert to haléře at the controller (`round(czk*100)`), rewrite `fine_form_controller.js` constants/preview to koruny (6000 / 2000·days / max(0.25%·base·days, 250·days)), update help text. `Fine` entity + `IssueFineCommand` stay haléře. | ready | [069-fine-form-koruny-input.md](069-fine-form-koruny-input.md) |

