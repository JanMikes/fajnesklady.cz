{# Renders the standard "Export do Excelu" link.
   Required vars: href (string).
   Pass already-resolved href; callers control which query params to merge in. #}
<a href="{{ href }}" class="btn btn-outline btn-sm">
    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-6m3 6v-2m3 2v-4M3 5a2 2 0 012-2h14a2 2 0 012 2v14a2 2 0 01-2 2H5a2 2 0 01-2-2V5z" />
    </svg>
    Export do Excelu
</a>
