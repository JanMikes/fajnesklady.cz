<nav class="bg-primary fixed top-0 left-0 right-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <a href="{{ path('app_home') }}" class="flex items-center">
                        <img src="{{ asset('images/logo-white.svg') }}" alt="Fajné Sklady" class="h-8">
                    </a>
                </div>
                <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                    <a href="{{ path('app_home') }}#pobocky" class="text-white/70 hover:text-white inline-flex items-center px-1 pt-1 text-sm font-medium transition-colors">
                        Pobočky
                    </a>
                    <a href="{{ path('app_home') }}#kontakt" class="text-white/70 hover:text-white inline-flex items-center px-1 pt-1 text-sm font-medium transition-colors">
                        Kontakt
                    </a>
                </div>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:items-center sm:space-x-4">
                {% if not app.user %}
                    <a href="{{ path('app_login') }}" class="text-white/70 hover:text-white px-3 py-2 text-sm font-medium transition-colors">Přihlásit</a>
                    <a href="{{ path('app_register') }}" class="btn btn-primary">Registrace</a>
                {% else %}
                    {% if is_granted('ROLE_USER') %}
                        <a href="{{ path('portal_dashboard') }}" class="text-white/70 hover:text-white px-3 py-2 text-sm font-medium transition-colors">Portal</a>
                    {% endif %}
                    <div class="relative" x-data="{ open: false }">
                        <button @click="open = !open" @click.away="open = false" type="button" class="flex items-center space-x-2 text-sm focus:outline-none rounded-full">
                            <div class="h-8 w-8 rounded-full bg-accent text-white flex items-center justify-center font-medium">
                                {{ app.user.fullName|first|upper }}
                            </div>
                        </button>
                        <div x-show="open" x-cloak class="origin-top-right absolute right-0 mt-2 w-48 rounded-md shadow-lg py-1 bg-white ring-1 ring-black ring-opacity-5 z-50">
                            <div class="px-4 py-2 text-xs text-gray-500 border-b border-gray-200">
                                {{ app.user.email }}
                            </div>
                            <a href="{{ path('app_logout') }}" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Odhlásit</a>
                        </div>
                    </div>
                {% endif %}
            </div>
            <div class="-mr-2 flex items-center sm:hidden">
                <button type="button" @click="mobileMenuOpen = !mobileMenuOpen" class="inline-flex items-center justify-center p-2 rounded-md text-white/70 hover:text-white hover:bg-white/10 focus:outline-none">
                    <span class="sr-only">Otevřít menu</span>
                    <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>
    </div>
    <div x-show="mobileMenuOpen" x-cloak class="sm:hidden bg-primary border-t border-white/10">
        <div class="pt-2 pb-3 space-y-1">
            <a href="{{ path('app_home') }}#pobocky" class="text-white/70 hover:text-white hover:bg-white/10 block pl-3 pr-4 py-2 text-base font-medium">Pobočky</a>
            <a href="{{ path('app_home') }}#kontakt" class="text-white/70 hover:text-white hover:bg-white/10 block pl-3 pr-4 py-2 text-base font-medium">Kontakt</a>
        </div>
        <div class="pt-4 pb-3 border-t border-white/10">
            {% if not app.user %}
                <div class="space-y-1">
                    <a href="{{ path('app_login') }}" class="block px-4 py-2 text-base font-medium text-white/70 hover:text-white hover:bg-white/10">Přihlásit</a>
                    <a href="{{ path('app_register') }}" class="block px-4 py-2 text-base font-medium text-white/70 hover:text-white hover:bg-white/10">Registrace</a>
                </div>
            {% else %}
                <div class="flex items-center px-4">
                    <div class="flex-shrink-0">
                        <div class="h-10 w-10 rounded-full bg-accent text-white flex items-center justify-center font-medium">
                            {{ app.user.fullName|first|upper }}
                        </div>
                    </div>
                    <div class="ml-3">
                        <div class="text-base font-medium text-white">{{ app.user.fullName }}</div>
                        <div class="text-sm font-medium text-white/70">{{ app.user.email }}</div>
                    </div>
                </div>
                <div class="mt-3 space-y-1">
                    {% if is_granted('ROLE_USER') %}
                        <a href="{{ path('portal_dashboard') }}" class="block px-4 py-2 text-base font-medium text-white/70 hover:text-white hover:bg-white/10">Portal</a>
                    {% endif %}
                    <a href="{{ path('app_logout') }}" class="block px-4 py-2 text-base font-medium text-white/70 hover:text-white hover:bg-white/10">Odhlásit</a>
                </div>
            {% endif %}
        </div>
    </div>
</nav>
