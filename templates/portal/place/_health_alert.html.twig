<div class="bg-amber-50 border border-amber-200 rounded-lg p-4 flex items-center gap-3">
    <div class="bg-amber-100 text-amber-600 rounded-full w-8 h-8 flex items-center justify-center flex-shrink-0">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4.5c-.77-.833-2.694-.833-3.464 0L3.34 16.5c-.77.833.192 2.5 1.732 2.5z" />
        </svg>
    </div>
    <div class="flex-1">
        <p class="text-sm font-medium text-amber-800">{{ title }}</p>
        <p class="text-xs text-amber-600">{{ detail }} <a href="{{ ctaUrl }}" class="underline font-medium">{{ ctaLabel }}</a>.</p>
    </div>
</div>
