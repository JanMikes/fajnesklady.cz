{% extends 'portal/layout.html.twig' %}

{% block title %}Upravit typ skladu - Admin{% endblock %}

{% block content %}
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold text-gray-900">Upravit typ skladu</h1>
        <a href="{{ path('portal_storage_types_list') }}" class="btn btn-ghost">Zpět na seznam</a>
    </div>

    <div class="max-w-2xl">
        <div class="card mb-6">
            <div class="card-body">
                <div class="flex items-center gap-4">
                    <div class="flex-shrink-0">
                        <div class="bg-primary text-white rounded-full w-16 h-16 flex items-center justify-center">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
                            </svg>
                        </div>
                    </div>
                    <div>
                        <h2 class="text-xl font-bold text-gray-900">{{ storageType.name }}</h2>
                        <p class="text-gray-600">
                            Vnitřní: {{ storageType.innerDimensionsInMeters }} ({{ storageType.floorAreaInSquareMeters|number_format(2, ',', ' ') }} m&sup2;, {{ storageType.volumeInCubicMeters|number_format(2, ',', ' ') }} m&sup3;)
                        </p>
                        {% if storageType.hasOuterDimensions %}
                            <p class="text-gray-500 text-sm">Vnější: {{ storageType.outerDimensionsInMeters }}</p>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>

        {# Existing photos #}
        {% if storageType.hasPhotos %}
            <div class="card mb-6">
                <div class="card-body">
                    <h3 class="text-lg font-semibold text-gray-900 mb-4">Nahrané fotografie</h3>
                    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                        {% for photo in storageType.photos %}
                            <div class="relative group">
                                <img src="{{ asset('uploads/' ~ photo.path) }}"
                                     alt="{{ storageType.name }}"
                                     class="w-full h-32 object-cover rounded-lg">
                                <form action="{{ path('portal_storage_type_photo_delete', {storageTypeId: storageType.id, photoId: photo.id}) }}"
                                      method="post"
                                      class="absolute top-2 right-2 opacity-0 group-hover:opacity-100 transition-opacity"
                                      onsubmit="return confirm('Opravdu chcete smazat tuto fotografii?');">
                                    <button type="submit"
                                            class="bg-red-600 hover:bg-red-700 text-white rounded-full p-1.5 shadow-lg">
                                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                                        </svg>
                                    </button>
                                </form>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        {% endif %}

        <div class="card">
            <div class="card-body">
                {{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
                    <div class="mb-4">
                        <label for="{{ form.name.vars.id }}" class="block text-sm font-semibold text-gray-700 mb-2">Název</label>
                        {{ form_widget(form.name, {'attr': {'class': 'input input-bordered w-full'}}) }}
                        {{ form_errors(form.name) }}
                    </div>

                    {# Inner dimensions (required) #}
                    <div class="mb-4">
                        <label class="block text-sm font-semibold text-gray-700 mb-2">Vnitřní rozměry (cm)</label>
                        <div class="grid grid-cols-3 gap-4">
                            <div>
                                <label for="{{ form.innerWidth.vars.id }}" class="block text-xs text-gray-500 mb-1">Šířka</label>
                                {{ form_widget(form.innerWidth, {'attr': {'class': 'input input-bordered w-full', 'placeholder': '200'}}) }}
                                {{ form_errors(form.innerWidth) }}
                            </div>
                            <div>
                                <label for="{{ form.innerHeight.vars.id }}" class="block text-xs text-gray-500 mb-1">Výška</label>
                                {{ form_widget(form.innerHeight, {'attr': {'class': 'input input-bordered w-full', 'placeholder': '250'}}) }}
                                {{ form_errors(form.innerHeight) }}
                            </div>
                            <div>
                                <label for="{{ form.innerLength.vars.id }}" class="block text-xs text-gray-500 mb-1">Délka</label>
                                {{ form_widget(form.innerLength, {'attr': {'class': 'input input-bordered w-full', 'placeholder': '300'}}) }}
                                {{ form_errors(form.innerLength) }}
                            </div>
                        </div>
                    </div>

                    {# Outer dimensions (optional) #}
                    <div class="mb-4">
                        <label class="block text-sm font-semibold text-gray-700 mb-2">Vnější rozměry (cm) - volitelné</label>
                        <div class="grid grid-cols-3 gap-4">
                            <div>
                                <label for="{{ form.outerWidth.vars.id }}" class="block text-xs text-gray-500 mb-1">Šířka</label>
                                {{ form_widget(form.outerWidth, {'attr': {'class': 'input input-bordered w-full', 'placeholder': '210'}}) }}
                                {{ form_errors(form.outerWidth) }}
                            </div>
                            <div>
                                <label for="{{ form.outerHeight.vars.id }}" class="block text-xs text-gray-500 mb-1">Výška</label>
                                {{ form_widget(form.outerHeight, {'attr': {'class': 'input input-bordered w-full', 'placeholder': '260'}}) }}
                                {{ form_errors(form.outerHeight) }}
                            </div>
                            <div>
                                <label for="{{ form.outerLength.vars.id }}" class="block text-xs text-gray-500 mb-1">Délka</label>
                                {{ form_widget(form.outerLength, {'attr': {'class': 'input input-bordered w-full', 'placeholder': '310'}}) }}
                                {{ form_errors(form.outerLength) }}
                            </div>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label class="block text-sm font-semibold text-gray-700 mb-2">Ceny (v CZK)</label>
                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <label for="{{ form.defaultPricePerWeek.vars.id }}" class="block text-xs text-gray-500 mb-1">Cena za týden</label>
                                {{ form_widget(form.defaultPricePerWeek, {'attr': {'class': 'input input-bordered w-full', 'placeholder': '0'}}) }}
                                {{ form_errors(form.defaultPricePerWeek) }}
                            </div>
                            <div>
                                <label for="{{ form.defaultPricePerMonth.vars.id }}" class="block text-xs text-gray-500 mb-1">Cena za měsíc</label>
                                {{ form_widget(form.defaultPricePerMonth, {'attr': {'class': 'input input-bordered w-full', 'placeholder': '0'}}) }}
                                {{ form_errors(form.defaultPricePerMonth) }}
                            </div>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label for="{{ form.description.vars.id }}" class="block text-sm font-semibold text-gray-700 mb-2">Popis</label>
                        {{ form_widget(form.description, {'attr': {'class': 'textarea textarea-bordered w-full', 'rows': 3}}) }}
                        {{ form_errors(form.description) }}
                    </div>

                    <div class="mb-4">
                        <label class="flex items-center gap-3 cursor-pointer">
                            {{ form_widget(form.uniformStorages, {'attr': {'class': 'checkbox checkbox-primary'}}) }}
                            <span class="text-sm font-semibold text-gray-700">Všechna úložiště budou stejná</span>
                        </label>
                        <p class="text-xs text-gray-500 mt-1 ml-8">Nebude se zobrazovat mapa pro výběr konkrétního místa a budou mít jednotnou cenu</p>
                        {{ form_errors(form.uniformStorages) }}
                    </div>

                    <div class="mb-4">
                        <label for="{{ form.photos.vars.id }}" class="block text-sm font-semibold text-gray-700 mb-2">Přidat další fotografie</label>
                        {{ form_widget(form.photos, {'attr': {'class': 'file-input file-input-bordered w-full'}}) }}
                        <p class="text-xs text-gray-500 mt-1">Nahrajte fotografie skladu (JPEG, PNG, WebP, max 5 MB každá)</p>
                        {{ form_errors(form.photos) }}
                    </div>

                    <hr class="my-6 border-t border-gray-200">

                    <div class="flex justify-end gap-2">
                        <a href="{{ path('portal_storage_types_list') }}" class="btn btn-ghost">Zrušit</a>
                        <button type="submit" class="btn btn-primary">Uložit změny</button>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
